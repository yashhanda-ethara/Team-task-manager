[build]
builder = "nixpacks"

[deploy]
startCommand = "flask --app run:app db upgrade && gunicorn run:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
